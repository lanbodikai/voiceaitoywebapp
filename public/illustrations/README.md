# Story illustrations

Add each production PNG using the `illustrationAsset` value from `src/data/stories.json` as its filename. For example, `scene_call_a_friend` becomes `scene_call_a_friend.png`.

The web app loads these files automatically and uses the story-specific emoji fallback when an illustration is unavailable.
